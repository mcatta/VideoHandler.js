# VideoHandler.js

Javascript to handle a HTML5 &lt;video> Tag.
Simple/easy to use.

```
var video = $('#video1').VideoHandler({
  onEnd: function() {
    // callback when video is ended
  }
});
video.play();
```
